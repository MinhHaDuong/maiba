# git-erg compiled binary
tickets/tools/go/erg

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/

# uv (commit uv.lock for reproducibility)
.venv/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code worktrees (created on demand by EnterWorktree / orchestrator)
.claude/worktrees/

# Local secrets
.env
.env.local

# Zotero-exported PDFs alongside the ground-truth RIS — too large for git
# (267 MB across 222 files). The RIS itself IS committed; the L1 paths
# resolve relative to the RIS's directory, so any test that follows the
# L1 links needs a local copy of files/ next to the RIS.
tests/fixtures/ArchiveCCS-cleaned/files/
